import expression.*;

public class Main {

    public static void main(String[] args) {
        //int ans = new Add(new Subtract(new Multiply(new Variable("x"), new Variable("x")), new Multiply(new Const(2), new Variable("x"))), new Const(1)).evaluate(Integer.parseInt(args[0]), Integer.parseInt(args[1]), Integer.parseInt(args[2]));
        //double ans = new Const (5.1).evaluate(5.1);
        //int ans = new Add(new Variable("x"), new Variable("y")).evaluate(Integer.parseInt(args[0]), Integer.parseInt(args[1]), Integer.parseInt(args[2]));
       // System.out.println(ans);
    }
}
